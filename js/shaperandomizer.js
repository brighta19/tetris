function ShapeRandomizer() {
    this.timers = {
        cooldownI: new Timer(),
        cooldownJ: new Timer(),
        cooldownL: new Timer(),
        cooldownO: new Timer(),
        cooldownS: new Timer(),
        cooldownT: new Timer(),
        cooldownZ: new Timer(),
    }
}
